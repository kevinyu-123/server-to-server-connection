server:
  port: 8090
  servlet:
    context-path: /client

  tomcat:
    threads:
      max: 4 # 생성할 수 있는 thread의 총 개수
      min-spare: 2 # 항상 활성화 되어있는(idle) thread의 개수
    max-connections: 1 # 수립가능한 connection의 총 개수
    accept-count: 2 # 작업큐의 사이즈
    connection-timeout: 3000 # timeout 판단 기준 시간
